# @devigner-ui/icons-pro

## 2.2.0

### Minor Changes

- `Unify icon rendering behind a single canonical renderer, so SVG, Vue, Svelte, and React Native packages generate from built components instead of being parsed from source.`
